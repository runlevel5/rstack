--- a/src/x86/Ginit.c
+++ b/src/x86/Ginit.c
@@ -130,6 +130,17 @@ access_reg (unw_addr_space_t as, unw_regnum_t reg, unw_word_t *val, int write,
   unw_word_t *addr;
   ucontext_t *uc = ((struct cursor *)arg)->uc;
 
+  if (!uc)
+    {
+      /* When uc is NULL (e.g., during DWARF-based unwinding after the first
+         frame), we cannot read from the ucontext. Instead, delegate to
+         tdep_access_reg which reads from the cursor's saved register state.
+         This handles the case where dwarf_get() encounters a DWARF_LOC_TYPE_REG
+         location and calls back into access_reg.
+         See: https://github.com/libunwind/libunwind/issues/938 */
+      return tdep_access_reg ((struct cursor *)arg, reg, val, write);
+    }
+
   if (unw_is_fpreg (reg))
     goto badreg;
 
@@ -156,6 +167,14 @@ access_fpreg (unw_addr_space_t as, unw_regnum_t reg, unw_fpreg_t *val,
   ucontext_t *uc = ((struct cursor *)arg)->uc;
   unw_fpreg_t *addr;
 
+  if (!uc)
+    {
+      /* When uc is NULL, delegate to tdep_access_fpreg which handles
+         reading from the cursor's saved register state.
+         See: https://github.com/libunwind/libunwind/issues/938 */
+      return tdep_access_fpreg ((struct cursor *)arg, reg, val, write);
+    }
+
   if (!unw_is_fpreg (reg))
     goto badreg;
 
